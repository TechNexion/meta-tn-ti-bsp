# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-tn-am62x-bsp"
BBFILE_PATTERN_meta-tn-am62x-bsp = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-tn-am62x-bsp = "20"

LAYERDEPENDS_meta-tn-am62x-bsp = "core"
LAYERSERIES_COMPAT_meta-tn-am62x-bsp = "dunfell"
